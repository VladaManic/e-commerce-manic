import styled, {  } from 'styled-components';

export const AllWrap = styled.div`
		display: flex;
		justify-content: space-between;
		flex-wrap: wrap;
		width: 70%;
		margin-right: auto;
		margin-left: auto
`;